import { AcortarStrPipe } from './acortar-str.pipe';

describe('AcortarStrPipe', () => {
  it('create an instance', () => {
    const pipe = new AcortarStrPipe();
    expect(pipe).toBeTruthy();
  });
});
